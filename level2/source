
// WARNING: [rz-ghidra] Removing arg arg_4h because it doesn't fit into ProtoModel

void main(void)
{
    p();
    return;
}

// WARNING: [rz-ghidra] Removing arg arg_4h because it doesn't fit into ProtoModel

void p(void)
{
    uint32_t unaff_retaddr;
    char *src;
    int32_t var_ch;

    fflush(_stdout);
    gets(&src);
    if ((unaff_retaddr & 0xb0000000) == 0xb0000000)
    {
        printf("(%p)\n", unaff_retaddr);
        _exit(1);
    }
    puts(&src);
    strdup(&src);
    return;
}
